<UserControl x:Class="ShopSystem.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ShopSystem.ViewModels"
             xmlns:local="clr-namespace:ShopSystem.Helpers">
    <UserControl.DataContext>
        <vm:ProductViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Left: list + search -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Product Management" FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}"/>
                <TextBlock Text="Database: products.db" FontSize="12"
                           Foreground="{StaticResource MutedBrush}"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Style="{StaticResource ModernTextBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Search by name or category..."/>
                <Button Grid.Column="1" Content="&#x21BB;" Style="{StaticResource SecondaryButton}"
                        Command="{Binding LoadCommand}" Margin="6,0,0,0" Width="36"/>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
                <DataGrid Style="{StaticResource ModernGrid}"
                          ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"       Binding="{Binding Id}"       Width="45"/>
                        <DataGridTextColumn Header="Name"     Binding="{Binding Name}"     Width="*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="110"/>
                        <DataGridTextColumn Header="Price (B)" Binding="{Binding Price, StringFormat=N0}" Width="80"/>
                        <DataGridTextColumn Header="Stock"    Binding="{Binding Stock}"    Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Right: form -->
        <Border Grid.Column="1" Style="{StaticResource Card}" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextBrush}"
                           Margin="0,0,0,12"
                           Text="{Binding IsEditing,
                               Converter={x:Static local:BoolToStringConverter.Instance},
                               ConverterParameter='Edit Product|Add Product'}"/>

                <TextBlock Text="Product Name *" Style="{StaticResource FormLabel}"/>
                <TextBox Style="{StaticResource ModernTextBox}"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Category *" Style="{StaticResource FormLabel}"/>
                <TextBox Style="{StaticResource ModernTextBox}"
                         Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Price (B) *" Style="{StaticResource FormLabel}"/>
                <TextBox Style="{StaticResource ModernTextBox}"
                         Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Stock Qty" Style="{StaticResource FormLabel}"/>
                <TextBox Style="{StaticResource ModernTextBox}"
                         Text="{Binding Stock, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Description" Style="{StaticResource FormLabel}"/>
                <TextBox Style="{StaticResource ModernTextBox}" Height="60" TextWrapping="Wrap"
                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Content="Add Product" Style="{StaticResource SuccessButton}"
                        Command="{Binding AddCommand}" Margin="0,14,0,6"
                        Visibility="{Binding IsEditing,
                                     Converter={x:Static local:BoolToVisibilityConverter.FalseVisible}}"/>

                <Button Content="Save Changes" Style="{StaticResource PrimaryButton}"
                        Command="{Binding UpdateCommand}" Margin="0,0,0,6"
                        Visibility="{Binding IsEditing,
                                     Converter={x:Static local:BoolToVisibilityConverter.TrueVisible}}"/>

                <Button Content="Delete" Style="{StaticResource DangerButton}"
                        Command="{Binding DeleteCommand}" Margin="0,0,0,6"
                        Visibility="{Binding IsEditing,
                                     Converter={x:Static local:BoolToVisibilityConverter.TrueVisible}}"/>

                <Button Content="Clear" Style="{StaticResource SecondaryButton}"
                        Command="{Binding ClearCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
